# 충전률 측정(adc_oneshot_read)이 OLED 비트뱅 I2C 전송을 파괴 — 현재 TEMP_NO_CHARGE=1 로 비활성

## 요약

배터리 충전률 측정을 켜면 **수 분 내에 OLED 화면이 멈춘다**. 현재 `main/somfy_app.c` 의
`TEMP_NO_CHARGE = 1` 로 충전률 측정을 **비활성화한 상태**이며, 그래야만 화면이 안정적으로 유지된다.

## 원인

`_read_bat_mv()` 가 호출하는 `adc_oneshot_read()` 는 IDF 내부(`esp_adc/adc_oneshot.c`)에서
변환 전체를 `portENTER_CRITICAL(&rtc_spinlock)` 으로 감싸 **인터럽트를 끈 채** 폴링한다.
이를 **8회 연속** 실행하는 동안, CPU 가 곧 클럭인 **비트뱅 I2C 전송이 중간에 끊겨** OLED 로
가는 프레임이 깨진다. SSD1306 은 그 상태로 고착되고, 4핀 모듈이라 RES 핀이 없어
**전원을 완전히 차단하기 전에는 복구되지 않는다**(ESP32 리셋·재플래시로는 안 풀림).

## 근거 (실기 이분 탐색, COM7 / xiao-c6)

| 조건 | 결과 |
|---|---|
| 충전측정 ON + 버튼 RF ON | 수 분 내 화면 멈춤 |
| 충전측정 OFF + 버튼 RF OFF | 멈춤 없음 |
| **충전측정 OFF + 버튼 RF ON** | **멈춤 없음** → RF 는 무죄 |
| 충전측정 ON | 재현 |

계측(`[OLEDMON]`, 충전측정 OFF 상태): 전송 515,697건 중 **실패 0건**, 1시간 24분 무결.
충전측정 ON 이던 시기에는 실패율이 0.02% → 12.5% 로 악화되며 고착에 도달했다.

## 수정 방향 (미적용)

1. `_bbo_write()` 를 뮤텍스로 감싸 **모든 OLED 전송 경로**를 보호
   (현재 `oled_ui_i2c_lock` 은 `_fb_flush` 만 덮어, 패널 init·검출 probe·`_oled_send_cmds` 가 무방비)
2. `adc_oneshot_read()` 호출을 **8회 → 1~2회**로 축소하고 회차 사이에 `vTaskDelay` 를 넣어
   크리티컬 구간을 잘게 나눔
3. 배터리 측정 주기를 **5초 → 30초**로 늘려 충돌 확률 자체를 감소
4. 위 적용 후 `TEMP_NO_CHARGE = 0` 으로 원복하고, `[OLEDMON]` 의 실패 카운터로 검증

## 참고

- 관련 커밋: `9fbbc43`(격리 플래그 추가), `035043c`(계측 추가), `50a1227`(dirty-page 감축)
- 배경 문서: README 「OLED 구동 방식 & 화면 정책」 섹션
- ⚠️ 시리얼 로그는 **CP949** 인코딩 — UTF-8 로 grep 하면 한글 에러가 0건으로 오독된다
- 별개 이슈: 충전측정을 꺼도 **장시간(7시간) 뒤 고착**된 사례가 있어, ADC 는 가속 요인이고
  근본 대책은 모듈 VCC 를 GPIO+MOSFET 으로 제어해 자동 전원 리셋하는 것
