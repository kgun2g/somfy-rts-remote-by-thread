# [전부 해결됨] 배터리/충전 관련 이슈 모음 (2026-08-11)

> 이 문서는 **GitHub 이슈가 아니라 리포지토리 내 기록**이다(`gh` CLI 미설치로 등록 못 함).
> GitHub 에 올리려면 이 내용을 그대로 붙여 넣으면 된다.

배터리·충전 도메인에서 이번에 해결한 네 건을 시간순으로 기록한다.
전부 실기에서 재현 → 원인 확정 → 수정 → 재확인까지 마쳤다.

| # | 증상 | 상태 |
|---|---|---|
| 1 | 충전률 측정을 켜면 OLED 가 몇 분 내 고착 | ✅ 해결 |
| 2 | 배터리 미연결인데 **78%** 로 표시 | ✅ 해결 |
| 3 | USB 없이 배터리만 연결하면 **부팅 중 멈춤/반복 재부팅** | ✅ 해결 |
| 4 | 배터리 구동 시 % 가 **74~84% 로 튐** | ✅ 해결 |

---

## 1. 충전률 측정이 OLED 비트뱅 I2C 전송을 파괴

**증상**: 배터리 충전률 측정을 켜면 수 분 내에 OLED 화면이 멈춘다. SSD1306 은 4핀
모듈이라 RES 핀이 없어, 한 번 고착되면 ESP32 리셋·재플래시로도 풀리지 않았다.

**근거 (실기 이분 탐색, COM7 / xiao-c6)**

| 조건 | 결과 |
|---|---|
| 충전측정 ON + 버튼 RF ON | 수 분 내 화면 멈춤 |
| 충전측정 OFF + 버튼 RF OFF | 멈춤 없음 |
| **충전측정 OFF + 버튼 RF ON** | **멈춤 없음** → RF 는 무죄 |

**원인 — 락이 전송 경로를 다 덮지 못했다.** ESP32-C6 는 싱글코어라 "동시 실행"이
아니라 **선점**이 파괴 기전이다.

    btn_handler 10  >  somfy_app 4  >  oled_ui 3
                          ↑ ADC + 화면OFF/ON      ↑ OLED 비트뱅 flush

somfy_app(prio 4)이 oled_ui(prio 3)를 선점한다. 비트뱅은 **CPU 가 곧 클럭**이라
선점당한 전송은 SCL/SDA 가 중간 상태로 수백 µs 멈춰 SSD1306 상태머신이 고착된다.
`adc_oneshot_read()` 의 `portENTER_CRITICAL` 은 **악화 요인**이지 근본이 아니었다.

`oled_ui_i2c_lock()` 이 `_fb_flush` 만 감싸서 아래 두 경로가 **무방비**였다:

| 무방비 경로 | 호출자 | 빈도 |
|---|---|---|
| `_oled_send_cmds()` ← `oled_ui_set_display_on()` | somfy_app | 화면 자동 OFF/ON **10초마다** |
| `_bbo_probe()` ← `_oled_try_detect()` | flush 의 early-return 경로(락 잡기 **전**) | 미검출 시 5초마다 |

**수정**
1. 락 범위를 **`_bbo_write()` 전송 함수 자체로 이동** — 호출 지점이 늘어도 자동 보호
2. **재귀 뮤텍스로 변경** — `_fb_flush` 가 바깥 락을 쥔 채 `_bbo_write` 를 부르므로
   일반 뮤텍스면 자기 자신을 기다려 **즉시 데드락**
3. 뮤텍스 생성을 `oled_ui_init()` **맨 앞**으로 — NULL 이면 무보호로 통과하던 구간 제거
4. 락 타임아웃(200 ms) 시 **전송은 진행**하고 `g_bbo_lock_to_cnt` 로만 계수
5. `TEMP_NO_CHARGE` 1 → **0** (충전측정 재활성)

**측정 주기(5초)·표본 수(8회)는 바꾸지 않았다.** 줄이면 `_nobat_track` 의
"5분 창 / 반쪽당 30표본" 노이즈 상쇄 가정이 깨져 배터리 미연결 오판이 생긴다.

**검증**
- 시뮬: `sim/tools/adc_oled_mutex_sim.py` (8시드 × 10분) — 수정 전 **6/8회 고착
  (최빠른 80초)** → 수정 후 손상 0 / 고착 0 / 배터리 측정 952/952
- 실기 장시간(COM7): 콘솔 `cyc` 로 화면 100% 부하 + ADC 5초 주기 병행
  **4시간 45분 / 전송 257,562건 / 실패 0 / lockTO 0**, `present` 284샘플 전부 1,
  패닉·재부팅·워치독·버스복구 0건, free heap Δ-80 B

---

## 2. 배터리 미연결인데 78% 로 표시

**원인**: 배터리를 빼도 충전 IC(MCP73831)가 BAT+ 를 무부하로 **약 3,970 mV** 로 띄우는데,
이 값이 OCV-SoC 곡선상 정확히 78% 다.

```
3850mV=60%, 3980mV=80% 구간 보간
60 + 20 × (3970-3850)/(3980-3850) = 78.4% → 78%
```

**전압만으로는 실제 배터리 78% 와 구분 불가.** 유일한 단서는 "전압이 오르는가"
(무배터리 float = 안 오름 / 충전 중인 셀 = 오름)이고, 이 판정은 5분 창이 차야 나온다.
→ 부팅 후 **5분 23초** 동안 78% 가 보였다(실기 로그로 확인).

```
W (322394) [BAT] 배터리 판정: 미연결 → 0% 표시
           전반 3971mV(n30) 후반 3969mV(n30) 상승 -2mV (창O, 안오름O, USB=1)
```

**수정**: 첫 판정 전에는 `--%` 표시(`BAT_PCT_UNKNOWN`). 단 **애매할 때만** 숨긴다 —
float 창(3940~4010 mV) 밖이거나 USB 미연결이면 배터리 존재가 자명하므로 즉시 % 표시.
실제 배터리를 꽂은 사용자는 거의 대기가 없다.

**★창(5분)을 줄이면 안 된다.** 충전 상승률 ≈9.3 mV/5분 vs 판정 문턱 4 mV:

| 창 | 전반/후반 평균차 | 판정 |
|---|---|---|
| 3분 | 2.8 mV | **오판**(충전 중인 배터리를 "미연결"로) |
| 5분 | 4.7 mV | 안전 |

검증: `sim/tools/bat_pct_display_sim.py` (4시나리오 + 창 단축 위험표)

---

## 3. USB 없이 배터리만 연결하면 부팅 중 멈춤 / 반복 재부팅

**증상**: USB 를 빼고 배터리만 연결하면 부팅 화면에서 멈추고, 40초~1분마다 재부팅.
메인 화면에 도달하지 못한다. USB 를 꽂으면 정상 — 그래서 시리얼 로그를 볼 수 없다.

**진단 방법**: 전원이 끊겨도 남는 **NVS 부팅 단계 기록**(`main/boot_diag.c`)을 만들었다.
RTC RAM 은 배터리를 빼면 지워지므로 못 쓴다. app_main 경로와 somfy_app 경로를 **따로**
세고(두 태스크가 동시 진행하므로 단일 카운터로는 구분 불가), 실패한 부팅은 별도 키에
**영구 보관**한다(정상 부팅이 덮어쓰지 않도록). `bd` 콘솔 명령으로 언제든 조회.

**결정적 기록**

```
직전 부팅: ★부팅 미완료
  app_main = 8 (콘솔 명령 등록 완료)
  somfy_app = 3 (oled_ui_init 직전)  sub = 10 (OLED 검출 성공)
  uptime = 1,193ms
  bat = 3994 / 최저 3994mV       ← 배터리 정상, 처짐 0
  리셋사유 = 전원투입             ← 브라운아웃/패닉/워치독 아님
```

**탈락한 가설들** (전부 데이터로 배제)

| 가설 | 탈락 근거 |
|---|---|
| 전원 부족/브라운아웃 | 배터리 4.02 V, **최저치도 동일**(사그 0). C6 는 브라운아웃을 정확히 보고하는데 2.92 V 로 올려도 한 번도 안 찍힘 |
| Task WDT | 타임아웃 5초라 40~60초 주기와 불일치 |
| BAT_ADC 로직 | 실패 기록 하나가 `somfy_app=0`(태스크 미실행) — 코드가 아예 실행 안 됨 |
| I2C 스캔 NULL 핸들 | `sub=10`(검출 성공)이라 그 경로 미실행 |

**원인**: `app_main`(prio 1)과 `somfy_app`(prio 4)이 **동시에** 멈췄다. 우선순위가 다른
두 태스크가 같이 굳는다면 공통 자원이 막힌 것이고, 그건 **콘솔 출력**뿐이다.

```cpp
// esp_matter_console.cpp — CHIP shell 을 우선순위 5 로 띄운다
xTaskCreate(&ChipShellTask, "console", ..., 5, NULL);   // RunMainLoop()
```

```c
// esp_rom_usb_serial.c — 줄바꿈마다 호스트를 기다린다
void esp_rom_usb_serial_putc(char c) {
    usb_serial_device_tx_one_char(c);
    if (c == '\n') usb_serial_device_tx_flush();
}
```

보조 콘솔이 USB-Serial-JTAG(`CONFIG_ESP_CONSOLE_SECONDARY_USB_SERIAL_JTAG`)이라,
USB 호스트가 없으면 이 ROM 함수가 대기한다. **prio 5** 태스크가 여기서 늘어지면
somfy_app(4)·oled_ui(3)를 굶겨 부팅이 끝나지 않는다. USB 를 꽂으면 호스트가 FIFO 를
비워 즉시 끝나므로 증상이 사라진다 — 관측과 정확히 일치.

**수정**: **VBUS 가 실제로 있을 때만** CHIP shell 콘솔을 시작한다(`app_main.cpp`
`_usb_vbus_present()`). 콘솔은 개발용이고 `tx`/`sel`/`cyc`/`bd` 도 USB 전용이라
배터리 단독 동작에는 불필요하다.

판정에 `usb_serial_jtag_is_connected()` 를 쓰지 않은 이유: 그 API 는 SOF 패킷 기반이라
부팅 ~1초 시점엔 열거가 안 끝나 false 가 나올 수 있고, 그러면 **USB 개발 중에도 콘솔이
사라진다**. VBUS 분압(GPIO17, `BOARD_CHG_STAT_ACTIVE_HIGH=1`)은 꽂는 즉시 HIGH 다.

**결과**: 배터리 단독 정상 부팅 확인.

**부수 수정**: `_oled_i2c_scan()` 이 비트뱅 모드에서 `s_dev._i2c_bus_handle == NULL` 로
`i2c_master_probe` 를 112번 부르고 있었다(크래시는 없지만 에러 로그 112줄).
비트뱅 프로브로 교체했다 — 이번 증상의 원인은 아니었지만 같은 함정을 만들 수 있는 코드다.

---

## 4. 배터리 구동 시 % 가 74~84% 로 튐

**증상**: 81 → 82 → **74** → 84 → 81 → 83 → 81 → 82 %

**원인**: USB 전원일 땐 레일이 단단해 전압이 거의 안 변했다. 배터리로 바꾸면 BLE 광고·
RF 송신 순간 **실제로** 전압이 떨어진다. 5초 주기 측정이 그 순간에 걸리면 한 점이 크게
낮게 찍힌다. OCV 곡선상 이 구간은 **1% ≈ 10 mV** 라 100 mV 만 흔들려도 10%p 가 움직인다.

`_read_bat_mv()` 의 8회 평균은 수십 µs 안에 끝나 **같은 순간을 8번 재는 것**이라
ADC 노이즈만 줄일 뿐 부하 변동은 못 거른다. 게다가 평균은 이상치에 통째로 끌려간다.

**수정 (표시 경로에만)**
1. **중앙값 5주기** — 송신 순간에 걸린 점을 통째로 버린다(평균과 달리 안 끌려감)
2. **EMA(α=1/4)** — 남은 흔들림을 시간축으로 눌러 표시가 안 튀게 한다

`_nobat_track` 에는 **원본 값**을 준다 — 그쪽은 5분 창 30표본 통계라 평활하면
이슈 #2 의 판정 가정이 깨진다. 측정 주기·표본 수도 그대로 둔다.

**★EMA 를 1/16 mV 단위로 누적한다.** C 의 정수 나눗셈은 0 쪽으로 절단하므로,
1 mV 단위로 `ema += (med-ema)/4` 를 쓰면 차이가 1~3 mV 일 때 몫이 0 이 되어
**EMA 가 영영 안 움직인다**(고착). 파이썬의 `//` 는 내림이라 시뮬레이터를 그냥
옮겼으면 이 버그를 못 봤을 것이다 — `sim/tools/bat_pct_smooth_sim.py` 가 C 절단을
흉내내 잡아냈다.

**검증** (`sim/tools/bat_pct_smooth_sim.py`)

| 지표 | 원본 | 평활 |
|---|---|---|
| 진폭 | 19 %p | **9 %p** |
| 주기간 평균 변동 | 4.09 %p | **0.38 %p** |
| 최저값(송신 강하 노출) | 66 % | **75 %** |
| 방전 추종 지연 | — | 2 %p 이내 |

---

## 참고 — 이 도메인에서 되풀이된 함정

- **로그 인코딩은 캡처 경로마다 다르다.** pyserial 직접 캡처 = UTF-8,
  빌드 로그(`logs/*.log`) = CP949. 틀리게 디코드하면 한글 에러가 0건으로 오독된다.
- **"실패 0"은 정상의 증거가 아니다.** OLED 미검출이면 flush 를 건너뛰어 에러도
  안 생긴다. `present=1` 과 전송 카운터 증가를 함께 볼 것.
- **부팅 ~100 ms 구간 로그는 USB-JTAG 이 버린다.** 진단값은 나중에 다시 출력해야 한다
  (실제로 `boot_diag` 첫 시험에서 핵심 줄만 통째로 유실됐다).
- **진단 기록을 한 칸만 두면 증거가 날아간다.** 실패 직후의 정상 부팅이 덮어썼다
  (`boot_diag` 를 별도 `fail` 키 보관으로 재설계한 이유).
- **리셋 사유는 "그 부팅이 시작된 이유"** 지 "끝난 이유"가 아니다. 헷갈리면 오진한다.
